<?xml version="1.0" encoding="utf-8"?>
<!--
  Replace YOUR_GOOGLE_MAPS_API_KEY with a valid key for Maps SDK.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.evcharger">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">

        <!-- Google Maps API Key (injected from local.properties via resValue) -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".ui.activities.QRScannerActivity" />
        <activity android:name=".ui.activities.ReservationFormActivity" />
        <activity android:name=".ui.activities.BookingListActivity" />
        <activity android:name=".ui.activities.PendingBookingsActivity" />
        <activity android:name=".ui.activities.DashboardActivity" />
        <activity android:name=".ui.activities.HomeActivity" />
        <activity android:name=".ui.activities.ProfileActivity" />
        <activity android:name=".ui.activities.OperatorDashboardActivity" />
        <activity android:name=".ui.activities.ManageStationActivity" />
        <activity android:name=".ui.activities.SignupActivity" />

        <!-- Launcher activity with intent filter must declare exported on Android 12+ -->
        <activity
            android:name=".ui.activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>